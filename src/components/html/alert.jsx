import { Alert } from "@mui/material";

function AlertTop() {
    return (  
        <Alert severity="info">The website is not finished yet. It is currently in test mode</Alert>
    );
}

export default AlertTop;